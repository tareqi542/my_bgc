import 'package:flutter/material.dart';

class MyAdminstration extends StatelessWidget {
  const MyAdminstration({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('MyAdminstration'),
    );
  }
}
